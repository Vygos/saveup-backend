version: '3'
services:
  saveup-backend:
    build: .
    ports:
      - 80:5000
    container_name: 'saveupbackend'
    environment:
      - SPRING_PROFILES_ACTIVE
      - BD_URL
      - BD_USERNAME
      - BD_PASSWORD
